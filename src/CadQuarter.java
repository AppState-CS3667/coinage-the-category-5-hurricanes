/**
 * @author  coinage-the-category-5-hurricanes
 * @version 1.0
 * for the candadian Quarter.
 */
public class CadQuarter extends Coin {
/**
 * the constructor for cad Quarter.
 * @param fail for if the coin failed.
 */
    public CadQuarter(boolean fail) {
	super("CAD", 0.25, "Quarter", "Unknown", fail);
    }
}
