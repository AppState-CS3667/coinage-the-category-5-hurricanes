/**
 * @author coinage-the-category-5-hurricanes
 * @version 1.0
 * fifty cent coin for usd.
 */
public class FiftyCent extends FormatCoin {
/**
 * @param fail for the failure condtion.
 * constructor for FiftyCent.
 */
    public FiftyCent(boolean fail) {
	super("CAD", 0.50, "Fifty Cent", "Unknown", fail);
    }
}
